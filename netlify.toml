# Netlify config for Mhmd Chamas

[build]
  publish = "."
  command = ""

# Redirect API calls if using external backend (e.g. Render)
# Uncomment and set your backend URL when using Netlify + Render
# [[redirects]]
#   from = "/api/*"
#   to = "https://YOUR-APP.onrender.com/api/:splat"
#   status = 200
#   force = true
